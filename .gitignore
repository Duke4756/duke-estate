node_modules
dist
.env
.DS_Store
*.log

# Python virtualenv leftover
.venv

# Facebook login session (contains cookies — never commit)
server/fb-session.json
server/jsa-session.json
scratch-debug

# User-edited group list
server/groups.json

# User-edited keyword list
server/keywords.json

# Local search history (Excel rounds)
server/history/

# Local owner-listing database
server/owner-posts.json
server/data/
test-results/
playwright-report/

# Auto-post data (post sets, images, schedules) — local only, never shared
server/autopost/
server/backups/
